server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://go-todo-app-backend-service/api/;
  }

  location / {
    try_files $uri /index.html;
  }
}
